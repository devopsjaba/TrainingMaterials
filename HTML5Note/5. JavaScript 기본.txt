1. 시작하기 
    - 에크마스크립트(ECMAScript)
        - ES2, ES3,...ES5
        - ES2015 (ES6)
        - 자바스크립트를 기반으로 표준화된 스크립트 언어
    - 특징
        - 모든 웹 브라우저에서 작동함 
        - 프런트엔드 개발, 벡엔드 개발       

2. 자바스트립트로 HTML 요소 선택하기 
    1) document.querySelector()
        - 요소 선택하기 
            - 부모 요소 선택하기
            - 형제 요소 선택하기 
    2) document.querySelectorAll()
        - 요소를 모두 선택하기 

3. for of문 
    1) 형식
        for(let "반복하는 요소가 담길 변수" of "반복시킬 그룹") {
            반복 실행할 구문        // 그룹에 담겨 있는 요소의 개수만큼 자동 반복 
        }                

4. for문 
    1) 형식
        for(let "반복하는 순서가 담길 변수"; 반복할 횟수의 최대값; 증감 연산자) {
            반복 실행할 구문       // 현재 반복하고 있는 그룹의 순번 탐색 
        }        

5. 자바스크립트로 스타일 제어하기

6. 자바스크립트로 이벤트 연결하기 
    1) 클릭 이벤트 
        - 특정 요소에 .addEventListener() 문을 연결해서 클릭 이벤트 지정함
        - 요소명.addEventListener("이벤트명", (전달될 값) => {실행할 구문})
        - e.preventDefault()
            - 이벤트의 기본 기능을 실행하지 말라는 명령어임 
    2) 호버 이벤트 
    3) 반복되는 요소에 이벤트 연결 

7. 문자 안에 변수 삽입하기
    1) 문자를 따옴표가 아닌 백틱(`)으로 감싸줌 
    2) 문자안에서 ${변수}로 문자의 변수값을 유지할 수 있음.

8. 함수
    1) 정의 및 호출     

