1. 접근 제한자(지정자)
    1) 멤버 및 생성자 
        - public > protected > default > private

                                            사용 가능 범위
          --------------------------------------------------------------------------           
        - public      동일패키지의 모든 클래스 + 다른 패키지의 모든 클래스에서 사용 가능
          protected   동일패키지의 모든 클래스 + 다른 패키지의 자식 클래스에서 사용 가능
          default     동일패키지의 모든 클래스에서 사용 가능
          private     동일클래스에서 사용 가능  

    2) 클래스의 접근 지정자 
        - public, default 접근 지정자만  사용 가능   
        - 클래스를 default로 정의하면 다른 패키지에서 임포트가 불가능 해 사용할 수 없게 됨  

2. static 
    1) 인스턴스 맴버(instance member)
        - 멤버 앞에 static이 붙어 있지 않은 멤버 
        - 객체 안에 있을 때 사용할 수 있는 상태가 되는 멤버 
    2) 정적 멤버(static member) 
        - 멤버 앞에 static이 붙어 있는 멤버       

        class A {
            int m = 3;              //객체를 생성한 후 사용 가능
            static int n = 5;       //객체 생성 없이 사용 가능 
        }

          A a = new A();

    3) 인스턴스 메서드
        - 반드시 객체를 생성한 후에 사용할 수 있음

    4) 정적 메서드 
        - 클래스명으로도 바로 접근할 수 있음               
        - 객체의 생성없이 실행될 수 있음 
    
    5) 정적 메서드 내부에서는 정적 멤버만 사용할수 있음     

    6) 정적 초기화 블록 
        - 일반적으로 인스턴스 필드의 초기화 
            - 객체가 만들어지는 시점에 이뤄짐 
        - 정적 필드
            - 객체의 생성 이전에도 사용할 수 있어야 함
            - 생성자 호출되지 않은 상태에서도 초기화 할 수 있어야 함 
                - 생성자에서는 정적 필드를 초기화할 수 없음     
        - 정적 초기화 블록 
            - static { }
            - 클래스가 로딩될 때 가장 먼저 실행됨 

3. 퀴즈 
    1) 배열 요소의 최대값 구하시오.
        max = a[0];
        if (a[1] > max) max = a[1]
        if (a[2] > max) max = a[2]

4. 난수 생성
    1) java.util.Random 
        - seed(씨앗)라는 수의 값을 바탕으로 난수값 발생 
        - Random rand = new Random()    //seed를 임의로 생성
          Random rand = new Random(n)   //seed를 지정 
    2) 난수생성메서드 
        nextBoolean()
        nextInt()
        nextInt(n)              0 ~ n-1
        nextLong()
        nextDouble()            0.0 이상 1.0 미만
        nextFloat()            

5. 배열 요소를 역순으로 정렬하기
    1) {2, 5, 1, 3, 9, 6, 7}
      => {7, 6, 9, 3, 1, 5, 2}

        2, 5, 1, 3, 9, 6, 7
        -                 - 교환 1번째

        7, 5, 1, 3, 9, 6, 2               
           -           -    교환 2번째

        7, 6, 1, 3, 9, 5, 2   
              -     -       교환 3번째

        7, 6, 9, 3, 1, 5, 2       
                            교환 횟수 = 요소수/2 = 3
    2) 두 값의 교환 
                         t = x      x = y      y = t
        x   57           57           13         13

        y   13           13           13         57

        t    -           57           57         57